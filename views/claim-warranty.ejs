<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Warranty Claim Form</title>
    <style>
        form {
            max-width: 400px;
            margin: auto;
            height: 400px;
            padding: 1em;
            border: 1px solid #ccc;
            border-radius: 1em;
        }
        div + div {
            margin-top: 1em;
        }
        input {
            width: 100%;
            padding: 0.5em;
            box-sizing: border-box;
        }
        button {
            padding: 0.7em;
            color: #fff;
            background-color: orange;
            border: none;
            border-radius: 1em;
            cursor: pointer;
        }
        button:disabled {
            background-color: #ccc;
        }
        #statusSection {
            margin-top: 1em;
            max-width: 400px;
            margin-left: auto;
            margin-right: auto;
            padding: 1em;
            border: 1px solid #ccc;
            border-radius: 1em;
        }
        body, html {
            height: 100%;
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: #f0f0f0;
            width: 100%;
        }

        .navbar {
            width: 100%;
            background-color: #333;
            color: white;
            text-align: center;
            padding: 10px 0;
           margin-top: 0; /* Add margin at the bottom */
        }

        .container {
            width: 70%; /* Set the width of the container */
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        *
        {
           
        }

    </style>
</head>
<body>
    <div class="navbar">
        <h1>Check Your Device Warranty </h1>
    </div>

    <form id="warrantyClaimForm" style="margin-top: 2%;">
        <div>
            <label for="certificateId">Certificate ID:</label>
            <input type="text" id="certificateId" name="certificateId" required>
        </div>
        <div>
            <label for="phoneNumber">Phone Number:</label>
            <input type="tel" id="phoneNumber" name="phoneNumber" required>
        </div>
        <div>
            <label for="emailId">Email ID:</label>
            <input type="email" id="emailId" name="emailId" required>
        </div>
        <div>
            <label for="serialNumber">Serial Number:</label>
            <input type="text" id="serialNumber" name="serialNumber" required>
        </div>
        <div>
            <label for="message">Query:</label>
            <input type="text" id="query" name="query" required>
        </div>
        <div>
            <button type="submit">Submit</button>
        </div>
    </form>

    <div id="statusSection" >
        <h2>Check Your Warranty Claim Status</h2>
        <div>
            <label for="checkCertificateId">Certificate ID:</label>
            <input type="text" id="checkCertificateId" name="checkCertificateId" required>
        </div>
        <div>
            <button id="checkStatusButton">Check Status</button>
        </div>
        <div id="statusResult"></div>
    </div>

    <script>
        document.getElementById('warrantyClaimForm').addEventListener('submit', async function (event) {
            event.preventDefault();

            const formData = {
                certificateId: document.getElementById('certificateId').value,
                phoneNumber: document.getElementById('phoneNumber').value,
                emailId: document.getElementById('emailId').value,
                serialNumber: document.getElementById('serialNumber').value,
                status: 'submitted'
            };

            try {
                const response = await fetch('http://localhost:5000/claim-warranty', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    alert('Warranty claim submitted successfully');
                } else {
                    alert('Error submitting warranty claim');
                }
            } catch (error) {
                alert('Error submitting warranty claim');
            }
        });

        document.getElementById('checkStatusButton').addEventListener('click', async function () {
            const certificateId = document.getElementById('checkCertificateId').value;

            try {
                const response = await fetch(`http://localhost:5000/warrantyClaim/status/${certificateId}`, {
                    method: 'GET',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });

                if (response.ok) {
                    const result = await response.json();
                    document.getElementById('statusResult').innerText = `Status: ${result.status}`;
                } else {
                    document.getElementById('statusResult').innerText = 'Error checking status or certificate not found';
                }
            } catch (error) {
                document.getElementById('statusResult').innerText = 'Error checking status';
            }
        });
    </script>
</body>
</html>
