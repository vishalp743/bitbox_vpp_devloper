<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Check Warranty</title>
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: #f0f0f0;
            width: 100%;
        }

        .navbar {
            width: 100%;
            background-color: #333;
            color: white;
            text-align: center;
            padding: 10px 0;
            margin-bottom: 20px; /* Add margin at the bottom */
        }

        .container {
            width: 70%; /* Set the width of the container */
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .input-field {
            width: 205%; /* Set the input field width to 100% */
            padding: 10px;
            margin-left: -50%;
            font-size: 1.5em;
            border: 2px solid orange;
            border-radius: 5px;
            margin-bottom: 10px;
            box-sizing: border-box; /* Include padding and border in the element's total width and height */
        }

        .search-button {
            width: 100%; /* Set the button width to 100% */
            background-color: orange;
            color: white;
            border: none;
            padding: 10px 0; /* Remove horizontal padding */
            font-size: 1.5em;
            border-radius: 5px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center; /* Center the button content */
        }

        .search-button:hover {
            background-color: darkorange;
        }

        .green-button {
            width: 100%; /* Set the button width to 100% */
            background-color: orange;
            color: white;
            border: none;
            padding: 10px 0; /* Remove horizontal padding */
            font-size: 1.5em;
            border-radius: 5px;
            text-decoration: none;
            display: block;
            text-align: center;
            margin-top: 20px;
        }

        .green-button:hover {
            background-color: darkorange;
        }

        p {
            margin-top: -1%;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <h1>Check Your Device Warranty </h1>
    </div>

    <div class="container">
        <form action="/check-warranty2" method="GET">
            <input type="text" class="input-field" name="serial" pattern="[A-Za-z0-9]{10}" placeholder="Enter your Serial Number without any Special Character" required>
            <button type="submit" class="search-button">
                Check Your Warranty 
                <span class="search-icon">🔍</span>
            </button>
        </form>
        <br>
        <% if (typeof expiryDate !== 'undefined') { %>
            <% if (status === 'registered') { %>
                <div class="result"> <h1 style="color: green;">Expiry Date: <%= new Date(expiryDate).toDateString() %></h1></div>
            <% } else if (status === 'not_registered') { %>
                <div class="result1"> <h1 style="color: red;">Warranty not registered</h1></div>
            <% } %>
            <% } %>
    
        <a href="https://bitbox-vpp-devloper.onrender.com/register-warranty" class="green-button">Register Warranty</a>
   
        <p style="margin-top:10px;">If you Know Serial Number & Model - You may register your warranty here</p>

        <a href="https://bitbox-vpp-devloper.onrender.com/check-device" class="green-button">Check Your Device Info</a>
        
        <p style="margin-top:10px;"> To Know your device  Serial Number, Model Number, Warranty Info click here</p>
    </div>
</body>
</html>
